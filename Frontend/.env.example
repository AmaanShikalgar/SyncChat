# Local dev: leave unset, App.tsx defaults to ws://localhost:8080
# Production (Vercel): set this to your deployed backend's WebSocket URL, e.g.
# VITE_WS_URL=wss://your-backend-name.onrender.com
VITE_WS_URL=

# Local dev: leave both unset, App.tsx defaults to the local backend on :8080

# Production (Vercel): set both to your deployed backend, e.g.
# VITE_API_URL=https://your-backend-name.onrender.com
# VITE_WS_URL=wss://your-backend-name.onrender.com
VITE_API_URL=
VITE_WS_URL=
